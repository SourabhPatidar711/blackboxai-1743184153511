# MongoDB Atlas Connection
MONGODB_ATLAS_URI=mongodb+srv://your_actual_username:your_actual_password@your_actual_cluster.mongodb.net/disaster_response?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# Security Notes:
# 1. Replace the above values with your REAL credentials from MongoDB Atlas:
#    - your_actual_username: Database username from Atlas
#    - your_actual_password: Database password
#    - your_actual_cluster: Your cluster name (e.g. cluster0.abc123)
# 2. In MongoDB Atlas, ensure:
#    - IP is whitelisted (Security > Network Access)
#    - User has readWrite privileges (Security > Database Access)
# 3. Never commit this file to version control