# MongoDB Atlas Connection
# USE THE EXACT CONNECTION STRING FROM MONGODB ATLAS:
MONGODB_ATLAS_URI=mongodb+srv://username:password@cluster0.code.mongodb.net/disaster_response?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# Verification Steps:
1. The above URI must match EXACTLY what MongoDB Atlas provides
2. In MongoDB Atlas:
   - Database Access: User has readWrite privileges
   - Network Access: IP is whitelisted (0.0.0.0/0 for testing)
3. Never commit this file to version control