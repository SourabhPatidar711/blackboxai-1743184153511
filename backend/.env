# MongoDB Atlas Connection
# REPLACE THESE VALUES WITH YOUR ACTUAL CREDENTIALS:
MONGODB_ATLAS_URI=mongodb+srv://your_db_username:your_db_password@your_cluster_name.mongodb.net/disaster_response?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# Required Setup:
1. Get your connection string from MongoDB Atlas:
   - Database > Connect > Connect your application
2. Security requirements:
   - Create database user (Security > Database Access)
   - Whitelist IP address (Security > Network Access)
3. Never commit this file to version control